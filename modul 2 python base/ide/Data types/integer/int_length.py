# Работа с длиной строки и чисел — функция len()
# Задание 1
num = 789

num_length = len(str(num)) # Преобразовываем num в строку с использованием функции len()
print(num_length)

# Задание 2
large_number = 1234567890123

large_number_str = len(str(large_number)) # Преобразовываем num в строку с использованием функции len()
print(large_number_str)

# Задание 3
phone_number = 84951234567

phone_number_str = len(str(phone_number))
print(phone_number_str)